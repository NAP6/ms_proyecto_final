server :
  port : 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NAP6/ms_proyecto_final.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
